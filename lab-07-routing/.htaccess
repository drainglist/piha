RewriteEngine On
RewriteBase /drainglistpiha/lab-07-routing/

# Если файл существует - отдаём его
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Всё остальное перенаправляем на router.php
RewriteRule ^(.*)$ router.php [QSA,L]