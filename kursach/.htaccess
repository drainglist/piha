RewriteEngine On
RewriteBase /drainglistpiha/kursach

# Если файл существует – отдаём его
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Всё остальное на router.php
RewriteRule ^(.*)$ router.php [QSA,L]