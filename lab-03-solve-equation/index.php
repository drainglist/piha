<?php
// Исходное уравнение в виде строки, чтобы вывести его на экран как текст.
$equation = 'X + 3 = 7';

// Отдельные переменные, которые хранят математический знак и описание структуры.
$operator = '+';
$position = 'X находится слева от оператора';

// Выносим числа из уравнения в переменные, чтобы PHP мог производить математические расчёты.
$number = 3;
$result = 7;

// Вычисляем значение неизвестной переменной X на стороне сервера.
// По правилам математики: чтобы найти неизвестное слагаемое, нужно из суммы вычесть известное слагаемое.
$x = $result - $number;
?>

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Solve the equation</title>
    <!-- Подключение файла внешних стилей CSS, чтобы карточка и шапка выглядели красиво -->
    <link rel="stylesheet" href="./style.css">
</head>
<body>
    <header class="header">
        <!-- ПЕРВЫЙ ЛОГОТИП (СЛЕВА): Путь исправлен на относительный "logo.png" (без косой черты в начале).
             Теперь сервер XAMPP будет искать картинку прямо в папке текущей (третьей) лабораторной работы -->
        <img class="logo" src="logo.png" alt="logo">
        
        <div class="title">Домашняя работа: Solve the equation</div>
        
        <!-- ВТОРОЙ ЛОГОТИП (СПРАВА): Заменили пустой блок "header-spacer" на вторую картинку.
             Это сделает шапку симметричной, и на сайте будут красиво отображаться оба логотипа -->
        <img class="logo" src="logo.png" alt="logo">
    </header>

    <main class="main">
        <section class="card">
            <h1>Решение уравнения</h1>

            <!-- Выводим само уравнение, используя короткий тег вывода PHP вместо длинного echo -->
            <div class="equation">
                <?= $equation ?>
            </div>

            <div class="result">
                <p>
                    <strong>Оператор:</strong>
                    <?= $operator ?>
                </p>

                <p>
                    <strong>Расположение неизвестной переменной:</strong>
                    <?= $position ?>
                </p>

                <p>
                    <strong>Решение:</strong>
                    <!-- Показываем пользователю сам процесс подстановки чисел в формулу -->
                    X = <?= $result ?> - <?= $number ?>
                </p>

                <!-- Выводим итоговый математический ответ, который автоматически рассчитал PHP -->
                <p class="answer">
                    X = <?= $x ?>
                </p>
            </div>
        </section>
    </main>

    <footer class="footer">
        задание для самостоятельно работы
    </footer>
</body>
</html>
